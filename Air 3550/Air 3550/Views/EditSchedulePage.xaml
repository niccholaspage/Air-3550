<Page
    x:Class="Air_3550.Views.EditSchedulePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Air_3550.Views"
    xmlns:converters="using:Air_3550.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:Air_3550.Models"
    xmlns:data1="using:Air_3550.Repository"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >

    <Page.Resources>
        <converters:IntConverter x:Key="IntConverter"></converters:IntConverter>
    </Page.Resources>

    <Grid>

        <ListView ItemsSource="{x:Bind ViewModel.FlightsA, Mode=TwoWay}" HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="displayedList">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Flight">
                    <StackPanel Width="300">
                        <TextBlock  Grid.Column="2"
                    Grid.Row="2"
                    Style="{ThemeResource BodyTextBlockStyle}"
                    Margin="12,0,0,6">
                            <Run Text="Number: "/>
                            <Run Text="{x:Bind Number}"/>
                        </TextBlock>
                        <TextBlock  Grid.Column="1"
                    Grid.Row="1"
                    Style="{ThemeResource BodyTextBlockStyle}"
                    Margin="12,0,0,6">
                            <Run Text="FlightID: "/>
                            <Run Text="{x:Bind FlightId}"/>
                        </TextBlock>
                        <TextBlock  Grid.Column="1"
                    Grid.Row="1"
                    Style="{ThemeResource BodyTextBlockStyle}"
                    Margin="12,0,0,6">
                            <Run Text="Origin Airport Code: "/>
                            <Run Text="{x:Bind OriginAirport.Code}"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Click="AddFlight_Click" HorizontalAlignment="Right">Add Dialog</Button>
            <Button Click="RemoveFlight_Click" HorizontalAlignment="Right">Remove Flight</Button>
            <Button Click="EditFlight_Click" HorizontalAlignment="Right">Edit Flight</Button>
            <TextBlock Text="{x:Bind ViewModel.Feedback, Mode=OneWay }" HorizontalAlignment="Center" Foreground="Red"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
