<Page
    x:Class="Air_3550.Views.EditSchedulePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Air_3550.Views"
    xmlns:converters="using:Air_3550.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:Air_3550.Models"
    xmlns:data1="using:Air_3550.Repository"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >

    <Page.Resources>
        <converters:IntConverter x:Key="IntConverter"></converters:IntConverter>
    </Page.Resources>

    <Grid>

        <ListView ItemsSource="{x:Bind FlightsA}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Flight">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="{x:Bind Number}" HorizontalAlignment="Left"/>
                        <TextBlock Text="{x:Bind FlightId}" HorizontalAlignment="Left"/>
                        <TextBlock Text="{x:Bind DestinationAirport.State}" HorizontalAlignment="Left"/>
                    </StackPanel>
            </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Click="AddFlight_Click" HorizontalAlignment="Right">Add Dialog</Button>
        </StackPanel>
    </Grid>
</Page>
