<Page
    x:Class="Air_3550.Views.PaymentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Air_3550.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:util="using:Air_3550.Util"
    xmlns:converters="using:Air_3550.Converters" xmlns:controls="using:Air_3550.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"></converters:BoolToVisibilityConverter>
    </Page.Resources>

    <RelativePanel Margin="5 45 0 0" >
        <StackPanel Orientation="Horizontal" x:Name="PaymentInfo">
            <StackPanel>
                <ContentControl x:Name="DepartureFlightPathControl" ContentTemplate="{StaticResource PaymentListViewTemplate}"/>
                <ContentControl x:Name="ReturnFlightPathControl" ContentTemplate="{StaticResource PaymentListViewTemplate}" Visibility="{x:Bind ViewModel.IsReturnFlight, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Height="200" Width="250" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="65" />
                    <RowDefinition Height="65" />
                    <RowDefinition Height="65" />
                    <RowDefinition Height="65" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Width="250" Padding="7">
                    <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center"  Grid.Row="0" 
                           Text="Departure Flight Cost:"/>
                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="1">
                            <Run Text="$"/>
                            <Run Text="{x:Bind ViewModel.DepartingFlightPath.Price}"/>
                    </TextBlock>
                </Grid>
                <Grid Grid.Row="1" Width="250" Padding="7">
                    <TextBlock Visibility="{x:Bind ViewModel.IsReturnFlight, Converter={StaticResource BoolToVisibilityConverter}}"
                           HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Row="0" 
                           Text="Return Flight Cost:"/>
                    <TextBlock Visibility="{x:Bind ViewModel.IsReturnFlight, Converter={StaticResource BoolToVisibilityConverter}}"
                           HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="1">
                        <Run Text="$"/>
                        <Run Text="{x:Bind ViewModel.ReturnFlightPath.Price}"/>
                    </TextBlock>
                </Grid>
                <Grid Grid.Row="2" Width="250" Padding="7">
                    <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Row="0" 
                    Text="Total Flight Cost:"/>
                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="1">
                        <Run Text="$"/>
                        <Run Text="{x:Bind ViewModel.TotalCost}"/>
                    </TextBlock>
                </Grid>
            </Grid>
        </StackPanel>
        <RelativePanel RelativePanel.AlignRightWith="PaymentInfo" RelativePanel.Below="PaymentInfo">
            <TextBlock Margin="2" RelativePanel.LeftOf="PaymentMethodDropdown" RelativePanel.AlignVerticalCenterWith="PurchaseButton">Payment Method:</TextBlock>
            <controls:PaymentTypeDropDownButton Margin="2" x:Name="PaymentMethodDropdown" RelativePanel.LeftOf="PurchaseButton" RelativePanel.AlignVerticalCenterWith="PurchaseButton"></controls:PaymentTypeDropDownButton>
            <Button Margin="2" x:Name="PurchaseButton" RelativePanel.AlignRightWithPanel="true">Purchase</Button>
        </RelativePanel>
    </RelativePanel>
</Page>
